<?xml version="1.0" encoding="utf-8"?>
<!-- dev only: the backend runs on a LAN IP over plain HTTP. Never selected for staging/prod. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
