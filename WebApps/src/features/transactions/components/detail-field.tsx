/** Promoted to `shared/components/detail-field.tsx` once Inventory's detail dialogs needed the same pair — re-exported so existing imports keep working. */
export { DetailField, MonoValue } from "@/shared/components/detail-field";
